<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.springboot.mapper.ReadingMapper">

    <select id="listByCondition" resultType="com.library.springboot.entity.Reading">
        select * from reading
        <where>
            <if test="name != null and name != ''">
                name like concat('%', #{ name }, '%')
            </if>
            <if test="bookNo != null and bookNo != ''">
                and book_no = #{bookNo}
            </if>
        </where>

        order by id desc

    </select>

    <insert id="save">
        insert into reading(name, description, favourite, cover, author, publisher, category, book_no)
        values(#{name},#{description},false,#{cover},#{author},#{publisher},#{category},#{bookNo})
    </insert>

    <update id="updateById">
        update reading set name = #{name}, description = #{description}, favourite = #{favourite},
        cover = #{cover}, author = #{author}, publisher = #{publisher}, category = #{category}, book_no = #{bookNo},
        updatetime = #{updatetime} where id = #{id}
    </update>

    <delete id="deleteById">
        delete from reading where id = #{id}
    </delete>

    <select id="list" resultType="com.library.springboot.entity.Reading">
        select * from book where reading = true order by id desc
    </select>


    <select id="getById" resultType="com.library.springboot.entity.Reading">
        select * from reading where id = #{id}
    </select>




</mapper>
